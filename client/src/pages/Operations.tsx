// Operations.tsx is now a thin shim. The implementation has been moved to
// ./operations/OperationsPage.tsx so the page can be split into smaller
// components/utilities under client/src/pages/operations/ over future passes.
// Behavior is unchanged.
export { default } from "./operations/OperationsPage";
