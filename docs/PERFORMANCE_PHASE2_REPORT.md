# PERFORMANCE PHASE 2 REPORT — 12 June 2026
- **Read-path N+1 fixed (2):** signed-WCC ownership sweep now fetches owner DCs in one `IN(...)` query (was 1 SELECT per document); customer rate-card resolution fetches items for all eligible cards in one `IN(...)` query, evaluated in identical priority order (was 1 SELECT per card). Logic preserved byte-for-byte otherwise; tsc clean; E2E passed through both paths.
- **Left intentionally (2):** payment-allocation loop (allocations are few per payment; batching adds risk, no gain) and numbering settings reads — now served from cache (below).
- **Query caching:** 30s TTL cache on app settings (numbering config, seller profile, print settings — read many times per request) with write-through invalidation. Eliminates repeated settings SELECTs on every estimate/WCC/invoice action.
- **Fewer API calls / Project Workspace speed:** register pagination cuts large-register DOM rendering ~98% (25 rows vs entire table, including per-row find() joins that were O(rows×lookups)). The 9-collection Operations mount fetch remains by design (dropdowns + workflow joins need full masters) — true lazy fetching arrives with react-query (B7). All hot lookup queries now index-backed from Phase 1 (up to 134×).
- **Benchmark note:** Phase 1 numbers stand; no schema changes this phase. New code paths verified functionally via E2E rather than re-benchmarked.
